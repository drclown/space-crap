<nav id="nav-space-crap" class="navbar navbar-inverse">
    <ul class="nav navbar-nav">
        {% for remote in remotes %}
            <li><a href="#">{{ remote.getRealpath }}</a></li>
        {% endfor %}
    </ul>
</nav>
<script>
$( document ).ready(function() {
    $("#nav-space-crap ul li").on("click", function () {
        var pathRemote = $(this).find("a").html();
        createTreeFiles(pathRemote);
    });
});
</script>